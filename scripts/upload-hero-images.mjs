#!/usr/bin/env node
/**
 * Script de migração (substituído).
 *
 * O conteúdo original deste arquivo era uma migração pontual de imagens de
 * hero do cliente anterior, contendo tokens assinados do Supabase Storage e
 * referências a buckets/URLs específicos do cliente.
 *
 * Ele foi neutralizado nesta branch por conter dados/credenciais do cliente
 * e NÃO faz parte do template genérico. Para fazer upload de heroes no novo
 * template, use o Admin (upload) ou os scripts de otimização contidos em
 * `scripts/` que leem configuração por variáveis de ambiente.
 */
console.log('Script de migração removido do template (neutralizado).')